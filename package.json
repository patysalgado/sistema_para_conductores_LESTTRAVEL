{
  "name": "sistema-registro-conductores",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "npx prisma db push --accept-data-loss && next start -p ${PORT:-3000} -H 0.0.0.0",
    "lint": "next lint",
    "db:push": "prisma db push",
    "db:seed": "node prisma/seed.js",
    "whatsapp": "node worker.js",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.12.0",
    "next": "^14.2.15",
    "react": "^18",
    "react-dom": "^18",
    "whatsapp-web.js": "^1.23.0",
    "qrcode-terminal": "^0.12.0",
    "node-cron": "^3.0.3",
    "jsonwebtoken": "^9.0.2",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {
    "prisma": "^5.12.0"
  }
}
